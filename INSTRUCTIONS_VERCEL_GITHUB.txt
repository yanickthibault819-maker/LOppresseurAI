INSTRUCTIONS — Déploiement sur Vercel via GitHub (L'Oppresseur AI v2.2 stable)

Date: 2026-01-31

Docs Vercel (officielles):
- Node runtime /api functions: https://vercel.com/docs/functions/runtimes/node-js
- Environment Variables: https://vercel.com/docs/environment-variables

⚠️ Important (clés API):
- En production (Vercel), configure tes clés dans Vercel → Project Settings → Environment Variables
- Vercel ne garde pas un .env “écrit par l'app” de façon persistante (filesystem immutable en serverless).

A) GitHub
1) Dézippe le dossier
2) Terminal dans le dossier:
   git init
   git add .
   git commit -m "L'Oppresseur AI v2.2 vercel"
3) Crée un repo vide sur GitHub et copie l'URL.
4) Push:
   git remote add origin <URL_DU_REPO>
   git branch -M main
   git push -u origin main

B) Vercel
1) Vercel → Add New Project → Import Git Repository
2) Sélectionne ton repo
3) Framework Preset: Other
4) Build & Output:
   - Output Directory: public
   - Install Command: npm install
   - Build Command: (vide)
5) Deploy

C) Variables d’environnement (Vercel)
Vercel → Project → Settings → Environment Variables
Ajoute (selon le provider):
- GEMINI_API_KEY
- OPENAI_API_KEY
- ANTHROPIC_API_KEY
Puis Redeploy.

D) Utilisation
1) Onglet 🔑 api → clique “tester” (ça détecte les modèles)
2) Onglet 🔥 générateur → choisis provider + model → générer

E) DEV local (si tu veux utiliser des champs clé à l'écran)
1) npm install
2) cp server-local/.env.example server-local/.env
3) mets tes clés dans server-local/.env
4) npm run dev
5) http://localhost:8787
