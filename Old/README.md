# L'Oppresseur AI — v2.2 stable (Vercel-ready)

UI: `/public`  
Backend (Vercel Functions): `/api`

## Déploiement
Voir `INSTRUCTIONS_VERCEL_GITHUB.txt`.

## DEV local
```bash
npm install
cp server-local/.env.example server-local/.env
npm run dev
```
